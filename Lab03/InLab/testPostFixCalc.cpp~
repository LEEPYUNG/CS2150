/* 
	Name: Pyung Lee
	Email ID: pkl4fr
	Date:2/3/2020
	File Name: testPostfixCalc.cpp
*/

#include "postfixCalculator.h"
#include <stack>
#include <cstdlib>
#include <iostream>
using namespace std;

int main(){
  
  postfixCalculator p;

  //Addition
  /*
  p.pushNum(1);
  p.pushNum(2);
  p.pushNum(3);
  p.pushNum(4);
  p.pushNum(5);
  p.add();
  p.add();
  p.add();
  p.add();

  cout << "Result is: " << p.myStack.top() << endl; 
  */

  //Subtraction
  /*
  p.pushNum(1);
  p.pushNum(2);
  p.pushNum(3);
  p.pushNum(4);
  p.pushNum(5);
  p.subtract();
  p.subtract();
  p.subtract();
  p.subtract();

  cout << "Result is: " << p.myStack.top() << endl; 
  */

  //Multiply
  /*
  p.pushNum(1);
  p.pushNum(2);
  p.pushNum(3);
  p.pushNum(4);
  p.pushNum(5);
  p.multiply();
  p.multiply();
  p.multiply();
  p.multiply();

  cout << "Multiply Result is: " << p.myStack.top() << endl; 
  */

  //Division
  /*
  p.pushNum(50);
  p.pushNum(6);
  p.pushNum(30);
  p.pushNum(100);
  p.pushNum(10);
  p.divide();
  p.divide();
  p.divide();
  p.divide();

  cout << "Result is: " << p.myStack.top() << endl; 
  */
  
  //Negation
  p.pushNum(1);
  p.negate();
  p.negate();
  p.negate();
  p.negate();

  cout << "Result is: " << p.myStack.top() << endl; 
  


  return 0;
}
